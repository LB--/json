language: cpp
compiler:
  - clang
  - gcc

sudo: true

install:
  - wget http://apt.biicode.com/install.sh && chmod +x install.sh && ./install.sh
  - bii --version
  - bii setup:cpp

before_script:
  - bii init . -L
  - bii find
  - bii configure -t

script:
  - bii build
  - ./bin/LB_json_test
